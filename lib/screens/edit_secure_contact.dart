import 'package:flutter/material.dart';

class EditSecureContact extends StatelessWidget {
  const EditSecureContact({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
